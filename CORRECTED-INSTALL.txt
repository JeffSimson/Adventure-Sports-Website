Adventure Sports Corrected Control Center

FIXED:
- Dashboard now appears when Decap opens at #/.
- Greeting waits for Netlify Identity and uses the signed-in user profile or email.
- Instant-update function uses strong consistency.
- Every live write is read back and revision-verified before the admin says Live now.
- Visible live connection status and Check Live button.
- Diagnostics page at /admin/diagnostics.html.
- Public app.js is cache-busted on every page.

INSTALL:
Replace every file in the repository, commit, push, and wait for a successful Netlify deploy. Then open /admin/?v=21 and hard refresh.
