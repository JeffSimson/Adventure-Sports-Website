const {json,verifiedUser,requireRole}=require('./_role-auth');const {requireStepUp,profile}=require('./_security');exports.handler=async event=>{try{const actor=await verifiedUser(event,{allowUnverified:true});requireRole(actor,['owner']);requireStepUp(event,actor);if(event.httpMethod!=='GET')return json(405,{error:'Method not allowed.'});const p=await profile(actor.user);return json(200,{ok:true,profile:{email:actor.user.email,email_mfa_enabled:true,last_verified_at:p?.last_verified_at||null}})}catch(e){return json(e.statusCode||500,{error:e.message,code:e.code})}};
