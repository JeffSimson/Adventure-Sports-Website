# V9.0.1 Login Hotfix

Fixes `Cannot set properties of null (setting textContent)` during sign-in.

The V9 Command Center now waits until authentication and the user profile are fully loaded before modifying dashboard elements. It also safely handles dashboard elements that are not present.

No Supabase SQL is required. Deploy with Clear cache and deploy site.
