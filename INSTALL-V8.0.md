# Install V8.0

Do not deploy until Resend can send from `SECURITY_EMAIL_FROM`.

Run migrations 005 and 006 in Supabase SQL Editor, add all required Netlify environment variables, deploy with cache cleared, then test Owner, Manager, Grounds, Kitchen, and Cashier accounts.
